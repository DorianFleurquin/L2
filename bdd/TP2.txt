1)
	SELECT ADHERENT.nomAdherent,ADHERENT.prenomAdherent FROM ADHERENT;
2)
	SELECT DISTINCT FILM.titreFilm FROM FILM; 
3)
	SELECT ADHERENT.idAdherent FROM ADHERENT WHERE ADHERENT.nomAdherent = 'Pierre DUCHEMIN';			
4)
	SELECT ADHERENT.nomAdherent, ADHERENT.prenomAdherent, ADHERENT.adresseAdherent FROM ADHERENT WHERE ADHERENT.nomAdherent LIKE '%DUCHEMIN';
5)
	SELECT ADHERENT.nomAdherent, ADHERENT.prenomAdherent, ADHERENT.adresseAdherent FROM ADHERENT JOIN ABONNEMENT ON ADHERENT.idAbo = ABONNEMENT.idAbo WHERE ABONNEMENT.nomAbonnement = 'VIP';
6)

